package Example4.schema;

import com.fasterxml.jackson.databind.Module;

import java.util.ArrayList;

public class Journal {

    /**
     * This version
     **/
    private String Name;


    /**
     * <T><h2>Scary Noise</h2></T></br>
     * This name
     * version
     ***/
    private String version;

    /**
     * Uncle Bob:
     * So, How's your `copy/paste doing?
     **/

    Journal() {
        /** imagine: a journal, with a subsystem
         *  **/
        //   List dependencies
        ArrayList<Module> moduleDependencies;
    }

    /**
     * <h2>Noise Comment</h2>
     * <p>the following is an example of noisy comments<br>
     * Please do not Journal  <br>
     * now we writ journals , and upload them onto Git < br>
     * (I hope you don't put them into the beginning of your source file ) < br>
     * you can get rid of it, now. < br>
     * - But what if we deleted it ? < br>
     * - It would still be in git.
     * <p>
     * who are you talking to, again >
     * <p>
     * I'm a java programmer
     *
     * </p>
     */

//Default Constructor - oh really!
    public void serialDate() {
    } // (+)
    /***
     //So, we have a new module
     public void  x (smodule){   //<-- does this even work ? (~) replace with `module`
     Module smodule = new Module(); // a java lang module , (+) To implement methods
     ***/

    //ListDecorator<>
    /**
     *  <unclear comment>* <p>Q.Does the module,
     *  from the global list mod is
     *   * Q2. Is it standalone?
     *   * </p> </unclear comment>
     *
     Does the angle bracket depend on the subsystem ( we are part of )

     if()
     }
     class smodule(){
     smodule() {
     getSystem();
     }
     }
     }

     Ask yourself:

     2. Does it depend on the subSystem ( We are a part of ?
     (I  mean, it could be ... anything!)
     Module dependencies
     from s.module
     3. also, `Extracted` :
     the other part (of the subsystem )
     `subsysmod`.get subsystem
     (

     4. oh, also, do not yell in the comment, Please do not

     <predicate>  it is appropriate to put some flag </predicate>
     <predicate> also , use that for instance variables

     </predicate>
     **/
    // contains(subSystem){ }

};